version: '3.4'

volumes:
  db-data:

services:
  db:
    image: postgres:12
    ports:
      - '127.0.0.1:${DB_PORT}:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
    env_file:
      - .env

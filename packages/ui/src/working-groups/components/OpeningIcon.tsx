import styled from 'styled-components'

import { Colors } from '@/common/constants'

export const OpeningIcon = styled.div`
  width: 96px;
  height: 96px;
  margin: 0 auto;
  border-radius: 50%;
  background-color: ${Colors.Black[50]};
`

import styled from 'styled-components'

import { Colors } from '@/common/constants'

import { RouterLink } from '../RouterLink'

export const ActivityRouterLink = styled(RouterLink)`
  font-size: 14px;
  line-height: 20px;
  font-weight: 400;
  text-decoration: none;
  color: ${Colors.Black[900]};
  font-weight: 700;
`

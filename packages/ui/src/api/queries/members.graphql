fragment MemberFields on Member {
  id
  rootAccount
  controllerAccount
  handle
#  name TODO - can't stub this field
  about
  avatarURI
  isFoundingMember
  isVerified
  inviteCount
}

query GetMembers {
  members {
    ...MemberFields
  }
}
